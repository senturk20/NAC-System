# ============================================================
# Python Dependencies for the Policy Engine API
# ============================================================

fastapi>=0.115.0,<1.0.0
uvicorn>=0.34.0,<1.0.0

# python-multipart: required by FastAPI to parse form-encoded POST bodies
# WHY: FreeRADIUS rlm_rest sends data as "application/x-www-form-urlencoded"
# and FastAPI needs this library to decode request.form()
python-multipart>=0.0.18,<1.0.0

# psycopg2-binary: PostgreSQL driver for Python
# WHY "binary": includes pre-compiled C libraries so we don't
# need to install build tools (gcc, libpq-dev) in our slim image
psycopg2-binary>=2.9.0,<3.0.0

# redis: Python client for Redis
redis>=5.0.0,<6.0.0

# jinja2: HTML template engine used by FastAPI to render the dashboard
jinja2>=3.1.0,<4.0.0

# pytest: test runner for our unit tests
pytest>=8.0.0,<9.0.0

# httpx: HTTP client required by FastAPI's TestClient
httpx>=0.27.0,<1.0.0
